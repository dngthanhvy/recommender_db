version: '3.8'
services:
  recommender_db:
    image: postgres:latest
    restart: always
    environment:
      - POSTGRES_PASSWORD=${PASSWORD}
      - POSTGRES_DB=${DB_NAME}
    ports:
      - ${PORT}:5432
    volumes:
      - ./schema_init.sql:/docker-entrypoint-initdb.d/init.sql
volumes:
  db:
    driver: local